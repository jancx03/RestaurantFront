import { createStore } from 'vuex';

const state = () => {};

const getters = {};

const actions = {};

const mutations = {};

export default createStore({
  state,
  getters,
  actions,
  mutations,
});
